{
    "compilerOptions": {
        "baseUrl": ".", // 或者 "./src", "./frontend" 等，取决于你的源码根目录
        "paths": {
            "@ui/*": [
                "ui/*"
            ] // 定义 @ui 指向 ui 目录
        // 如果你的源码在 frontend 目录下，可能是这样：
            // "@ui/*": ["frontend/ui/*"]
        }
    },
}